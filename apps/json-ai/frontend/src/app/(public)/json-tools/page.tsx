import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Tools — Online JSON Formatter, Validator & Converter",
  description:
    "Free online JSON toolkit. Format, validate, minify, compare JSON, and convert JSON to TypeScript, SQL, CSV, and JSON Schema — all in one place. AI-powered JSON repair included.",
};

export { default } from "@/views/JsonTools";
