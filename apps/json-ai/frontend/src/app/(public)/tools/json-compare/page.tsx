import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Compare Diff",
  description: "Compare two JSON objects.",
};

export { default } from "@/views/tools/JsonCompare";