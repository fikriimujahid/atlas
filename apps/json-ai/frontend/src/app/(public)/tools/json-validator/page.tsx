import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Validator",
  description: "Validate your JSON online.",
};

export { default } from "@/views/tools/JsonValidator";