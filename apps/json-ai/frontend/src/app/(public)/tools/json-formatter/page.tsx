import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Formatter & Beautifier",
  description: "Format, beautify, and validate your JSON data instantly.",
};

export { default } from "@/views/tools/JsonFormatter";