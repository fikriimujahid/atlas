import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "JSON Tools — Format, Validate, Minify, Convert",
  description:
    "All-in-one JSON toolkit. Format, validate, minify, compare, and convert JSON to TypeScript, SQL, CSV, and JSON Schema — all in one page.",
};

export { default } from "@/views/JsonTools";
